# 開發紀錄與下次接續

> 這份文件給「下一次打開這個專案」的自己或 AI 助手看。
> ⚠️ **家庭代碼不寫在這裡**（那是資料的鑰匙，這個 repo 是公開的）。
> 忘記的話到 App 的「家長 → 設定 → 家庭代碼」查看。

---

## 專案是什麼

給小朋友 Cookie（低年級，7-9 歲）用的每日任務打勾 PWA。
iPad 加到主畫面後就像原生 App，家長手機也能同步查看。

| 項目 | 內容 |
| --- | --- |
| 線上網址 | <https://pp4646.github.io/cookie-daily-task/> |
| 程式碼 | <https://github.com/pp4646/cookie-daily-task>（Public） |
| 資料庫 | Firebase Firestore，專案 `cookie-s-daily-task`（個人 Gmail 登入） |
| 部署 | GitHub Pages，`git push` 後約 1 分鐘自動上線 |
| 目前版本 | 見 `js/version.js` |

**技術選擇**：純前端（HTML/CSS/原生 JS ES Modules），沒有編譯步驟、沒有 npm。
改完直接 `git push` 就好。資料層抽象成 `js/store.js`，未填 Firebase 設定時
會自動退回 localStorage，方便離線開發。

---

## 2026-09-01 這天做了什麼

從零到上線，共 7 個 commit、5 個版本。

### v1.1.0 注音
- 用 pypinyin + OpenCC 離線產生對照表（20,924 單字 + 8,649 詞彙）。
  pypinyin 詞庫是簡體的，先用 OpenCC 轉繁體才能對應台灣用字。
- **詞彙優先比對**處理破音字：「倒垃圾」`ㄉㄠˋ ㄌㄜˋ ㄙㄜˋ`、
  「彈鋼琴」`ㄉㄢˋ`、「家長」`ㄓㄤˇ`（但「長度」仍是 `ㄔㄤˊ`）。
- 加上台灣教育部讀音修正表，蓋掉 pypinyin 的大陸讀音。
- 「一」「不」變調在前端即時計算（「一本書」→ `ㄧˋ`、「一件事」→ `ㄧˊ`）。
- 注音直排在國字右側、聲調在右下、輕聲 `˙` 在正上方（台灣課本排法）。
- 全面指定繁體中文字型，另提供標楷體選項。

### v1.2.0 家庭代碼記不住
- **Bug**：原本等 Firebase 連線成功才記住代碼，連線慢就沒存到。改成一輸入就記住。
- **iOS**：Safari 會清掉 PWA 的儲存空間。改成同時把代碼寫進網址 `#f=`，
  主畫面圖示會連網址一起記住，清掉也還原得回來。
- 新增「複製邀請連結」與「更換家庭代碼」。

### v1.3.0 換成真實任務
預設任務改成 Cookie 實際在做的 6 項（抽成 `js/store.js` 的 `COOKIE_TASKS`）。

### v1.4.0 自動更新
- **問題**：Service Worker 為了離線會優先給快取，改版後常要開第 2、3 次才生效。
- **解法**：每次開啟與切回前景時直接跟伺服器問版本號（不走快取），
  不同就跳出「有新版本，點一下更新」，點下去清快取重載，一次到位。
- Service Worker 加例外放行版本檢查請求。

### v1.5.0 家長專區自動上鎖
- **問題**：解鎖後就一直開著，iPad 給小孩就能亂改。
- **解法**：「✓ 完成，鎖定並離開」按鈕 + 三種自動上鎖
  （切分頁 / 切到背景 / 閒置 3 分鐘）。

### 其他修掉的 bug
- 每日任務的 id 每次重繪都重新產生，導致**點了無法打勾**（改用模板 id）。
- 家長設定沒有任何儲存回饋，讓人以為沒存到（其實是自動儲存，補上提示）。
- 「完成」按鈕誤放在「設定」分頁裡，其他分頁看不到。

---

## 🔴 還沒做，下次優先

1. **改家長密碼** — 還是預設的 `1234`，Cookie 很快會猜到。
   家長 → 設定 → 家長密碼。
2. **換成真實獎品** — 目前還是預設的看卡通 10 點 / 去公園 20 點 / 選新書 40 點。
3. **檢查點數平衡** — 每天最多 4~5 點、一週約 30 點，
   現有獎品定價可能偏低，Cookie 會太快集滿。
4. **清掉 Firestore 測試資料** — `families` 底下的
   `codetest-*`、`smoketest-*`、`connftest-*`、`updtest-*`、`locktest-*`、`dbg-*`
   都是自動測試留下的，可以刪。
5. **把專案 clone 到個人位置** — 目前工作目錄在**公司的 Google Drive**
   (`G:\My Drive\Shared Work Drive\...`)，離職就沒了。
   GitHub 上有完整備份，但本機這份要搬。

## 🟡 可以考慮的功能

- 從備份 JSON 匯入（目前只能匯出）
- 每週 / 每月成果統計，或給 Cookie 看的成就徽章
- 任務排序（拖拉調整順序）
- 多小孩切換（架構已支援，UI 只在設定頁）

## ⚠️ 已知問題

- `tools/test_update.py` 連續與其他測試一起跑時，曾偶發失敗一次，
  但單獨與連續重跑 3 次都正常。判斷是測試腳本的時序問題，非產品 bug，尚未追根究柢。

## 🔒 安全注意事項

這個 repo 是 **Public**，所以：

- `js/config.js` 裡的 Firebase `apiKey` 是公開的 —— **這是正常的**，
  客戶端金鑰本來就會出現在網頁原始碼裡，不是密碼。
- **真正的鑰匙是「家庭代碼」**，它保護 Firestore 裡所有資料。
  安全規則要求：必須通過匿名登入 + 代碼長度 8~64 字。

### 🚨 絕對不要把家庭代碼寫進任何檔案

2026-09-01 曾經犯過這個錯：`tools/apply_tasks.py` 和 `tools/screenshot_live.py`
的說明文字裡寫了實際的家庭代碼，並且推上了公開 repo。
雖然當天就從檔案裡移除，但 **git 歷史紀錄仍然查得到**
（commit `69b5f5a`、`6a78ae4`），所以那組代碼應視為已外洩、必須更換。

以後代碼一律用命令列參數傳入，程式和文件裡只放 `<你的家庭代碼>` 佔位字樣。

---

## 開發方式備忘

```powershell
# 本機預覽（雙擊 start.cmd 也可以）
python -m http.server 8000 --bind 127.0.0.1
```

> 一定要用 `--bind 127.0.0.1`，否則 Windows 防火牆會跳出詢問視窗，
> 而公司電腦的群組原則會讓你按不下「允許」。localhost 走回送介面不經過防火牆。

### 自動測試

```powershell
python -m http.server 8765 --bind 127.0.0.1   # 另一個視窗

python tools/smoke_test.py         # 主要流程 17 步
python tools/test_family_code.py   # 家庭代碼記憶
python tools/test_update.py        # 自動更新機制
python tools/test_parent_lock.py   # 家長專區上鎖
python tools/test_firebase.py      # 雲端雙向即時同步
```

加 `$env:BASE_URL="https://pp4646.github.io/cookie-daily-task/"` 就會測線上版。

> Playwright 導覽請用 `wait_until="domcontentloaded"`。
> Firestore 是長連線，`networkidle` 永遠等不到。
> 重新啟動 App 要用 `page.reload()`，不能 `goto` 同一個含 hash 的網址
> （瀏覽器會當成同頁導覽，不會重跑開機流程）。

### 改版流程

1. 改程式
2. 更新 `js/version.js` 的 `VERSION` 與 `VERSION_DATE`
3. 在 `CHANGELOG.md` 最上面補一段
4. 跑測試
5. `git add -A && git commit -m "..." && git push`
6. 約 1 分鐘後上線，iPad 開 App 會跳出更新提示

---

## 下次的提示詞

複製下面整段貼給 AI 助手：

```text
我在維護一個給兒子 Cookie（低年級）用的每日任務打勾 PWA。

專案位置：這個工作目錄
線上網址：https://pp4646.github.io/cookie-daily-task/
GitHub：https://github.com/pp4646/cookie-daily-task （Public）
資料庫：Firebase Firestore，專案 cookie-s-daily-task

請先讀 下次繼續.md、CHANGELOG.md 和 README.md 了解現況，
再讀 js/ 底下的程式碼。

技術重點：
- 純前端，原生 JS ES Modules，沒有編譯步驟也沒有 npm
- js/store.js 是資料層，Firebase 沒設定時會自動退回 localStorage
- js/version.js 是唯一版本號來源，Service Worker 快取跟著它更新
- 注音對照表 js/zhuyin-data.js 由 tools/gen_zhuyin.py 產生，不要手改

我的要求：
- 改完一定要用 tools/ 底下的 Playwright 測試實際驗證，不要只說「應該可以」
- 每次改版都要更新 js/version.js 並在 CHANGELOG.md 記錄
- 用繁體中文回覆
- 本機伺服器一定要加 --bind 127.0.0.1（公司電腦防火牆政策）

這次我想做的是：
（在這裡寫你要做的事，例如「把獎品換成 XXX」「加上每週統計」）
```

---

## 檔案結構

```
start.cmd                一鍵啟動本機預覽
index.html               主頁面
manifest.json            PWA 設定
sw.js                    Service Worker（離線 + 自動更新）
firebase.json            Firebase Hosting 設定（目前用 GitHub Pages，沒在用）
firestore.rules          資料庫安全規則（已套用到線上）

css/style.css            全部樣式（含注音排版、繁體字型）

js/version.js            ★ 版本號（唯一來源）
js/config.js             ★ Firebase 設定
js/store.js              資料層 + COOKIE_TASKS 預設任務
js/app.js                開機、狀態管理、小孩端、自動更新
js/parent.js             家長專區 + 上鎖機制
js/ui.js                 共用工具（日期、彈窗、音效、動畫、注音包裝）
js/zhuyin.js             注音標注邏輯
js/zhuyin-data.js        注音對照表（自動產生，勿手改）

icons/                   App 圖示

tools/gen_zhuyin.py      產生注音對照表
tools/check_zhuyin.py    驗證對照表
tools/preview_zhuyin.py  預覽任意文字的注音
tools/gen_icons.py       產生 App 圖示
tools/apply_tasks.py     把 COOKIE_TASKS 寫進線上資料
tools/screenshot_live.py 線上畫面截圖
tools/smoke_test.py      主要流程測試
tools/test_family_code.py
tools/test_update.py
tools/test_parent_lock.py
tools/test_firebase.py
```
