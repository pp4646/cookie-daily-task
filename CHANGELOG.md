# 更新紀錄 (Changelog)

「Cookie 的每日挑戰」所有版本的修改內容。

版本號規則採 [語意化版本](https://semver.org/lang/zh-TW/) `主版本.次版本.修訂號`：

| 位置 | 什麼時候要加 | 例子 |
| --- | --- | --- |
| **主版本** | 大改版，舊資料需要轉換 | 1.4.2 → 2.0.0 |
| **次版本** | 新增功能，舊資料照常可用 | 1.1.0 → 1.2.0 |
| **修訂號** | 只修 bug 或調整外觀 | 1.1.0 → 1.1.1 |

> **改版時要做的兩件事**
> 1. 修改 `js/version.js` 裡的 `VERSION` 與 `VERSION_DATE`
> 2. 在本檔案最上方新增一段紀錄
>
> Service Worker 會用版本號當作快取名稱，所以只要版本號變了，
> iPad 上的 App 下次開啟時就會自動抓到新版。

---

## [1.3.0] - 2026-09-01

### 變更
- **預設任務改成 Cookie 實際在做的 6 項**（原本是範例用的刷牙、寫功課等）：

  | 任務 | 星期 | 點數 |
  | --- | --- | --- |
  | 🔤 上51英文課 | 二、四、五 | 1 |
  | 🎒 整理書包(文、聯、國、資、古、作) | 日、一、二、三、四 | 1 |
  | 😊 說出3件今天發生的好事 | 日、六 | 1 |
  | 📖 閱讀一本書 | 每天 | 1 |
  | 🧹 幫忙做家事 | 每天 | 1 |
  | 💪 說出今天控制住自己的一件事 | 每天 | 1 |

  任務清單抽成 `js/store.js` 的 `COOKIE_TASKS`，方便日後修改。
  ⚠️ 只影響**全新的家庭代碼**；已經在用的資料請在 App 的家長頁修改。
- 任務圖示選單增加 🔤 😊 💪 🗣️，對應「說出…」「控制自己」這類任務。

### 開發工具
- `tools/preview_zhuyin.py`：預覽任意文字的注音標注結果。
- `tools/apply_tasks.py`：把 `COOKIE_TASKS` 寫進線上的家庭資料。
  預設是唯讀預覽，要加 `--apply` 才會實際寫入。

---

## [1.2.0] - 2026-09-01

### 修正
- **每次開啟都要重新輸入家庭代碼**。兩個原因都處理了：
  1. 程式原本是「Firebase 連線成功之後」才記住代碼。只要連線慢或失敗，
     代碼就沒存到，下次開啟又要重打。改成**使用者一輸入就立刻記住**。
  2. iOS 會定期清掉加到主畫面的網頁儲存空間。現在代碼會**同時寫進網址的 `#f=`**，
     主畫面圖示會連網址一起記住，就算儲存被清掉也還原得回來。

### 新增
- **邀請連結**：家長 → 設定 → 「複製邀請連結」，產生像
  `https…/#f=你的代碼` 的網址。家人點開就自動設定好，不必手動輸入代碼。
  無法使用剪貼簿時（iOS 或非 HTTPS）會改用視窗顯示網址讓你手動複製。
- **更換家庭代碼**：家長 → 設定 → 「更換家庭代碼」可直接輸入新代碼，
  不用再清瀏覽器資料。會事先提醒換代碼等於切換到另一份資料。

### 調整
- 家長設定頁把「家庭代碼」獨立成一個區塊，和版本資訊分開。
- 移除用不到的 `forgetFamilyCode()`。

---

## [1.1.1] - 2026-09-01

### 修正
- **看不出來設定有沒有存到**。家長專區本來就是每改一次就立刻寫入，
  但畫面上沒有任何回饋，容易誤以為還要按儲存鍵。現在補上：
  - 分頁下方常駐一行提示「所有變更都會立刻自動儲存，不需要按儲存鍵」
  - 每個動作完成後跳出確認訊息，例如「已新增『閱讀一本書』」「已儲存 ✓」「已刪除」
  - 新增、編輯、刪除、開關、點數調整、改密碼、注音與字體切換都有回饋
- 家長頁的動作統一改用同一個儲存流程，避免有些操作忘記重繪畫面。

---

## [1.1.0] - 2026-09-01

### 新增
- **注音功能**：小孩看得到的中文字旁邊會顯示注音符號，方便 7-9 歲的小朋友閱讀與認字。
  - 注音以直排方式顯示在國字右側（台灣課本的排法），聲調符號放在注音右下，輕聲的 `˙` 放在最上方。
  - 內建 20,924 個單字 + 8,649 個詞彙的對照表，會**優先用詞彙比對**來處理破音字。
    例如「倒垃圾」讀 `ㄉㄠˋ ㄌㄜˋ ㄙㄜˋ`、「彈鋼琴」讀 `ㄉㄢˋ`、「家長」讀 `ㄓㄤˇ`。
  - 對照表以台灣教育部讀音為準，已修正 pypinyin 的中國大陸讀音差異
    （如「垃圾」`ㄌㄜˋ ㄙㄜˋ`、「星期」`ㄒㄧㄥ ㄑㄧˊ`、「頭髮」`ㄊㄡˊ ㄈㄚˇ`、「企鵝」`ㄑㄧˋ ㄜˊ`）。
  - 支援「一」「不」的變調（例：「一個」讀 `ㄧˊ ㄍㄜˋ`）。
- **注音手動修正**：家長在編輯任務或獎品時，可以自己指定注音，蓋掉自動判斷的結果。
- **注音開關**：家長設定頁可以關掉注音，等 Cookie 長大認得字之後就不需要了。
- **繁體字型**：全面指定台灣繁體中文字型（PingFang TC / 微軟正黑體 / Noto Sans TC），
  避免瀏覽器套用簡體或日文字型，導致「骨」「過」「直」「者」等字出現非繁體寫法。
- **楷體選項**：小孩看得到的文字可切換成**標楷體**，也就是台灣課本教寫字用的字體，
  對正在學寫字的小朋友筆畫比較清楚。
- **版本號機制**：`js/version.js` 為唯一版本來源，設定頁會顯示目前版本，
  Service Worker 快取也會跟著版本號更新。

### 開發工具
- `tools/gen_zhuyin.py`：用 pypinyin + OpenCC 產生注音對照表 `js/zhuyin-data.js`。
  pypinyin 的詞庫是簡體的，這支程式會先用 OpenCC 轉成繁體再建表，以保留破音字的覆蓋率。
- `tools/check_zhuyin.py`：驗證產生出來的對照表，用與前端相同的演算法做端對端比對。
- `tools/gen_icons.py`：產生 App 圖示。
- `tools/smoke_test.py`：用 Playwright 實際操作一遍 App（打勾、兌換、家長各分頁、
  新增任務、切換注音與字體、重新整理），並檢查有沒有 console 錯誤。

### 修正
- 每日任務的 id 原本每次重繪都會重新產生，導致點擊後找不到對應任務、
  無法打勾。改為直接沿用模板 id。
- 新增 `start.cmd` 一鍵啟動本機預覽，伺服器綁定 `127.0.0.1`。
  公司電腦的 Windows 防火牆若因群組原則無法勾選「允許存取」，
  這樣就不會跳出詢問視窗，本機開發也完全不受影響。

---

## [1.0.0] - 2026-09-01

第一個版本。

### 新增
- **小孩端**
  - 今日任務清單，大圖示大按鈕，點一下就打勾，有動畫與音效。
  - 完成度圓環、連續達成天數（🔥 streak）。
  - 可以往前翻看前幾天的紀錄。
  - 獎品商店：用累積的點數兌換家長設定的獎品，點數不夠會顯示還差幾點。
- **家長端**（用 4 位數 PIN 進入）
  - 每日固定任務模板，可指定星期幾出現。
  - 今日臨時任務（只出現一次，不重複）。
  - 獎品清單管理、兌換申請核准。
  - 月曆檢視每天的達成狀況，以及完成率統計。
  - 小孩管理（保留多個小孩的彈性）、手動加減點數、修改 PIN。
  - 匯出 JSON 備份。
- **資料同步**
  - 未填 Firebase 設定時使用瀏覽器本機儲存，開檔即可試用。
  - 填入 Firebase 設定後自動改用 Firestore，iPad 與家長手機用同一組「家庭代碼」即時同步。
- **PWA**
  - 可加入 iPad 主畫面，全螢幕執行，離線也能開啟。
